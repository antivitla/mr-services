const falseConfirm = require('./dialog-false-confirm');
const trueConfirm = require('./dialog-true-confirm');
const stringify = require('./dialog-stringify');
const confirmContentList = require('./dialog-confirm-content-list');
const confirmContentAndTitle = require('./dialog-confirm-content-and-title');

module.exports = {
  falseConfirm,
  trueConfirm,
  stringify,
  confirmContentList,
  confirmContentAndTitle,
};
